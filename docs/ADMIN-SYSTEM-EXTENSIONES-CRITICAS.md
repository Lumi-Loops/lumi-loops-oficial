# Extensiones Críticas Obligatorias - Sistema de Administración (Fase 4)\n\n**Fecha:** 2025-11-05 \n**Status:** INCORPORADO al Plan de Implementación Oficial \n**Prioridad:** CRÍTICA - Requisitos Técnicos Obligatorios \n\n---\n\n## 📋 INTEGRACIÓN AL PLAN EXISTENTE\n\nEstas extensiones se integran oficialmente al `PLAN-ADMIN-MANAGEMENT-SYSTEM.md` como **requisitos técnicos obligatorios** para garantizar completitud funcional del sistema administrativo.\n\n---\n\n## 1. 🎨 UI/UX Admin - Especificaciones Técnicas del Panel Web\n\n### 1.1 Bandeja de Tickets (\"Support Inbox\")\n\n**Componente:** `AdminTicketsInbox.tsx`\n\n`tsx\ninterface TicketListProps {\n  tickets: SupportTicket[];\n  onAssignToMe: (ticketId: string) => void;\n  onOpenTicket: (ticketId: string) => void;\n}\n\ninterface SupportTicket {\n  id: string;\n  inquiry_id: string;\n  status: 'new' | 'in_progress' | 'resolved' | 'closed';\n  priority: 'low' | 'normal' | 'high' | 'urgent';\n  assigned_to?: string;\n  visitor_name: string;\n  visitor_email: string;\n  message: string;\n  created_at: string;\n  updated_at: string;\n}\n`\n\n**Funcionalidades Requeridas:**\n- ✅ Vista tipo tabla con columnas: ID, Estado, Prioridad, Visitante, Fecha\n- ✅ Badges de color por estado:\n `css\n  .badge-new { background: #ef4444; } /* Rojo */\n  .badge-in-progress { background: #f59e0b; } /* Amarillo */\n  .badge-resolved { background: #10b981; } /* Verde */\n  .badge-closed { background: #6b7280; } /* Gris */\n  `\n- ✅ Filtros dinámicos: estado, prioridad, asignado/sin asignar\n- ✅ Botón \"Asignarme\" (solo visible si `assigned_to` es NULL)\n- ✅ Paginación para listas grandes\n- ✅ Refresh automático cada 30 segundos\n\n**Query SQL base:**\n`sql\nSELECT \n  st.id, st.status, st.priority, st.assigned_to, st.created_at, st.updated_at,\n  vi.name as visitor_name, vi.email as visitor_email, vi.message\nFROM support_tickets st\nJOIN visitor_inquiries vi ON st.inquiry_id = vi.id\nWHERE st.status != 'closed'\nORDER BY \n  CASE st.priority \n    WHEN 'urgent' THEN 1 \n    WHEN 'high' THEN 2 \n    WHEN 'normal' THEN 3 \n    WHEN 'low' THEN 4 \n  END,\n  st.created_at DESC;\n`\n\n---\n\n### 1.2 Formulario de Respuesta\n\n**Componente:** `AdminResponseForm.tsx`\n\n`tsx\ninterface ResponseFormProps {\n  ticketId: string;\n  onResponseSent: (responseId: string) => void;\n}\n\ninterface ResponseFormData {\n  response_text: string;\n  download_link?: string;\n  send_email: boolean;\n}\n`\n\n**Funcionalidades Requeridas:**\n- ✅ Editor de texto enriquecido (TinyMCE o similar)\n- ✅ Campo opcional para `download_link` con validación URL\n- ✅ Toggle \"Enviar email al usuario\" (default: true)\n- ✅ Preview del mensaje antes de enviar\n- ✅ Validaciones:\n `tsx\n  const validateResponse = (data: ResponseFormData) => {\n    if (!data.response_text.trim()) return \"Mensaje requerido\";\n    if (data.download_link && !isValidURL(data.download_link)) {\n      return \"URL de descarga inválida\";\n    }\n    return null;\n  };\n  `\n\n**Workflow al enviar:**\n1. Crear entrada en `admin_responses`\n2. Actualizar `support_tickets.updated_at`\n3. Si `send_email = true`, crear entrada en `admin_notifications_queue`\n4. Disparar trigger de notificación\n5. Registrar acción en `admin_audit_log`\n\n---\n\n### 1.3 Vista de Actividad del Usuario\n\n**Componente:** `UserActivityView.tsx`\n\n`tsx\ninterface UserActivity {\n  user_id: string;\n  last_appointment_date?: string;\n  last_appointment_status?: string;\n  last_payment_date?: string;\n  last_payment_status?: string;\n  last_download_date?: string;\n  last_download_status?: string;\n  total_appointments: number;\n  total_payments: number;\n  total_downloads: number;\n  last_login?: string;\n}\n`\n\n**Funcionalidades Requeridas:**\n- ✅ Cards con métricas visuales (iconos + números)\n- ✅ Timeline de actividad reciente\n- ✅ Botón \"Ver detalles\" que abre modal con datos completos\n- ✅ Indicadores de estado con colores\n- ✅ Actualización en tiempo real desde `user_activity_cache`\n\n**Query optimizada O(1):**\n`sql\nSELECT * FROM user_activity_cache \nWHERE user_id = $1;\n`\n\n---\n\n### 1.4 Registro de Auditoría (Solo super_admin)\n\n**Componente:** `AdminAuditLog.tsx`\n\n**Funcionalidades Requeridas:**\n- ✅ Tabla paginada con búsqueda y filtros\n- ✅ Columnas: Fecha, Admin, Acción, Target, Cambios\n- ✅ Filtros por:\n - Rango de fechas\n - Admin específico\n - Tipo de acción\n - Tipo de target\n- ✅ Export a CSV para análisis\n- ✅ Detalle expandible de cambios JSON\n\n---\n\n## 2. 🔄 Comportamiento del Sistema: Workflows Avanzados\n\n### 2.1 Respuestas Múltiples por Ticket\n\n**Requisito:** Un `support_ticket` DEBE poder tener múltiples `admin_responses`\n\n**Implementación en Base de Datos:**\n`sql\n-- Relación 1:N ya está implementada correctamente\n-- support_tickets(id) <- admin_responses(support_ticket_id)\n\n-- Trigger para actualizar ticket.updated_at con cada respuesta\nCREATE OR REPLACE FUNCTION update_ticket_on_response()\nRETURNS TRIGGER AS $$\nBEGIN\n  UPDATE support_tickets \n  SET updated_at = NOW()\n  WHERE id = NEW.support_ticket_id;\n  RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER update_ticket_timestamp_on_response \nAFTER INSERT ON admin_responses\nFOR EACH ROW EXECUTE FUNCTION update_ticket_on_response();\n`\n\n**Componente UI:** `TicketConversation.tsx`\n- Thread de conversación (cronológico)\n- Respuestas anteriores readonly\n- Formulario nuevo al final\n\n---\n\n### 2.2 Cambio de Asignación y Escalamiento\n\n**Workflow de Escalamiento:**\n`\nvisitor_inquiry → support_ticket (unassigned)\n    ↓ (assign)\nsupport_agent → (escalate) → support_manager → (escalate) → super_admin\n`\n\n**Implementación:**\n`sql\n-- Función para reasignar ticket con audit log\nCREATE OR REPLACE FUNCTION reassign_ticket(\n  p_ticket_id UUID,\n  p_new_assignee UUID\n) RETURNS BOOLEAN AS $$\nDECLARE\n  old_assignee UUID;\nBEGIN\n  -- Obtener asignado anterior\n  SELECT assigned_to INTO old_assignee \n  FROM support_tickets WHERE id = p_ticket_id;\n  \n  -- Actualizar asignación\n  UPDATE support_tickets \n  SET assigned_to = p_new_assignee, updated_at = NOW()\n  WHERE id = p_ticket_id;\n  \n  -- Registrar en audit log\n  INSERT INTO admin_audit_log (admin_id, action, target_type, target_id, changes)\n  VALUES (\n    auth.uid(),\n    'ticket_reassigned',\n    'ticket',\n    p_ticket_id,\n    jsonb_build_object(\n      'old_assignee', old_assignee,\n      'new_assignee', p_new_assignee\n    )\n  );\n  \n  RETURN TRUE;\nEND;\n$$ LANGUAGE plpgsql;\n`\n\n**Componente UI:** Dropdown \"Reasignar a\" con lista de admins activos\n\n---\n\n### 2.3 Cierre Automático de Tickets\n\n**Tabla extension:**\n`sql\nALTER TABLE support_tickets \nADD COLUMN auto_close_after_days INTEGER DEFAULT 7;\n`\n\n**Cron Job (implementar en API):**\n`sql\nCREATE OR REPLACE FUNCTION auto_close_resolved_tickets()\nRETURNS INTEGER AS $$\nDECLARE\n  closed_count INTEGER := 0;\nBEGIN\n  UPDATE support_tickets \n  SET status = 'closed', updated_at = NOW()\n  WHERE status = 'resolved' \n    AND updated_at < NOW() - INTERVAL '1 day' * auto_close_after_days;\n  \n  GET DIAGNOSTICS closed_count = ROW_COUNT;\n  \n  -- Log mass closure\n  INSERT INTO admin_audit_log (admin_id, action, target_type, target_id, changes)\n  VALUES (\n    NULL, -- Sistema\n    'tickets_auto_closed',\n    'system',\n    gen_random_uuid(),\n    jsonb_build_object('closed_count', closed_count)\n  );\n  \n  RETURN closed_count;\nEND;\n$$ LANGUAGE plpgsql;\n`\n\n---\n\n## 3. 🔔 Consola de Notificaciones Fallidas\n\n### 3.1 Vista Admin para `admin_notifications_queue`\n\n**Componente:** `NotificationQueueAdmin.tsx`\n\n`tsx\ninterface NotificationQueueItem {\n  id: string;\n  response_id: string;\n  recipient_user_id: string;\n  recipient_email: string; // JOIN con profiles\n  notification_type: string;\n  status: 'queued' | 'sending' | 'sent' | 'failed';\n  retry_count: number;\n  max_retries: number;\n  error_message?: string;\n  created_at: string;\n  sent_at?: string;\n}\n`\n\n**Funcionalidades Requeridas:**\n- ✅ Lista con filtros por estado\n- ✅ Badges de color por status\n- ✅ Acciones:\n - \"Reintentar\" (manual)\n - \"Marcar como no reenviar\"\n - \"Ver error completo\"\n- ✅ Stats dashboard:\n `tsx\n  const stats = {\n    total_queued: number,\n    total_failed: number,\n    success_rate: number,\n    avg_retry_count: number\n  };\n  `\n\n**API Endpoints necesarios:**\n`typescript\nPOST /api/admin/notifications/:id/retry\nPATCH /api/admin/notifications/:id/skip\nGET /api/admin/notifications/stats\n`\n\n---\n\n### 3.2 Trigger de Retry Automático\n\n`sql\nCREATE OR REPLACE FUNCTION schedule_notification_retry()\nRETURNS TRIGGER AS $$\nBEGIN\n  -- Solo si es un failure y hay retries disponibles\n  IF NEW.status = 'failed' AND NEW.retry_count < NEW.max_retries THEN\n    -- Programar retry en 5 minutos (implementar con cron o worker)\n    UPDATE admin_notifications_queue\n    SET \n      status = 'queued',\n      retry_count = retry_count + 1,\n      error_message = NULL\n    WHERE id = NEW.id;\n  END IF;\n  \n  RETURN NEW;\nEND;\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER notification_retry_trigger \nAFTER UPDATE ON admin_notifications_queue\nFOR EACH ROW EXECUTE FUNCTION schedule_notification_retry();\n`\n\n**Worker Process (Node.js/Bun):**\n`typescript\n// Ejecutar cada 5 minutos\nconst processNotificationQueue = async () => {\n  const queued = await supabase\n    .from('admin_notifications_queue')\n    .select('*')\n    .eq('status', 'queued')\n    .order('created_at', { ascending: true })\n    .limit(10);\n    \n  for (const notification of queued.data) {\n    try {\n      await sendEmail(notification);\n      \n      await supabase\n        .from('admin_notifications_queue')\n        .update({ status: 'sent', sent_at: new Date().toISOString() })\n        .eq('id', notification.id);\n    } catch (error) {\n      await supabase\n        .from('admin_notifications_queue')\n        .update({ \n          status: 'failed', \n          error_message: error.message \n        })\n        .eq('id', notification.id);\n    }\n  }\n};\n`\n\n---\n\n## 4. 📚 Documentación Final Obligatoria (Post-Fase 4)\n\n### 4.1 Diagrama ER Actualizado\n\n`mermaid\nerDiagram\n    support_tickets {\n        uuid id PK\n        uuid inquiry_id FK\n        varchar status\n        varchar priority\n        uuid assigned_to FK\n        int auto_close_after_days\n        timestamp created_at\n        timestamp updated_at\n    }\n    \n    admin_responses {\n        uuid id PK\n        uuid support_ticket_id FK\n        uuid admin_id FK\n        text response_text\n        varchar download_link\n        boolean email_sent\n        timestamp email_sent_at\n        boolean viewed_by_user\n        timestamp viewed_at\n        boolean link_clicked\n        timestamp link_clicked_at\n        timestamp created_at\n        timestamp updated_at\n    }\n    \n    admin_roles {\n        uuid id PK\n        uuid admin_id FK\n        varchar role_level\n        boolean can_manage_users\n        boolean can_manage_payments\n        boolean can_manage_admins\n        boolean can_view_all_analytics\n        varchar assigned_regions\n        timestamp created_at\n        timestamp updated_at\n    }\n    \n    admin_audit_log {\n        uuid id PK\n        uuid admin_id FK\n        varchar action\n        varchar target_type\n        uuid target_id\n        jsonb changes\n        inet ip_address\n        text user_agent\n        timestamp created_at\n    }\n    \n    user_activity_cache {\n        uuid id PK\n        uuid user_id FK\n        timestamp last_appointment_date\n        varchar last_appointment_status\n        timestamp last_payment_date\n        varchar last_payment_status\n        timestamp last_download_date\n        varchar last_download_status\n        int total_appointments\n        int total_payments\n        int total_downloads\n        timestamp last_login\n        timestamp updated_at\n    }\n    \n    admin_notifications_queue {\n        uuid id PK\n        uuid response_id FK\n        uuid recipient_user_id FK\n        varchar notification_type\n        varchar status\n        int retry_count\n        int max_retries\n        text error_message\n        timestamp created_at\n        timestamp sent_at\n    }\n    \n    support_tickets ||--|| visitor_inquiries : inquiry_id\n    support_tickets ||--o{ admin_responses : support_ticket_id\n    admin_responses ||--o{ admin_notifications_queue : response_id\n    profiles ||--o{ admin_roles : admin_id\n    profiles ||--|| user_activity_cache : user_id\n`\n\n---\n\n### 4.2 JSON Schema de Tablas\n\n**Archivo:** `docs/admin-tables-schema.json`\n\n`json\n{\n  \"support_tickets\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"id\": { \"type\": \"string\", \"format\": \"uuid\" },\n      \"inquiry_id\": { \"type\": \"string\", \"format\": \"uuid\" },\n      \"status\": { \n        \"type\": \"string\", \n        \"enum\": [\"new\", \"in_progress\", \"resolved\", \"closed\"] \n      },\n      \"priority\": { \n        \"type\": \"string\", \n        \"enum\": [\"low\", \"normal\", \"high\", \"urgent\"] \n      },\n      \"assigned_to\": { \n        \"type\": [\"string\", \"null\"], \n        \"format\": \"uuid\" \n      },\n      \"auto_close_after_days\": { \"type\": \"integer\", \"default\": 7 },\n      \"created_at\": { \"type\": \"string\", \"format\": \"date-time\" },\n      \"updated_at\": { \"type\": \"string\", \"format\": \"date-time\" }\n    },\n    \"required\": [\"id\", \"inquiry_id\", \"status\", \"priority\"]\n  }\n  // ... resto de las tablas\n}\n`\n\n---\n\n### 4.3 Tabla de Permisos por Rol\n\n| Acción | support_agent | support_manager | super_admin |\n|--------|---------------|----------------|-------------|\n| Ver tickets asignados | ✅ | ✅ | ✅ |\n| Ver todos los tickets | ❌ | ✅ | ✅ |\n| Responder tickets | ✅ | ✅ | ✅ |\n| Asignar tickets | ❌ | ✅ | ✅ |\n| Cambiar prioridad | ❌ | ✅ | ✅ |\n| Ver actividad usuarios | ✅ | ✅ | ✅ |\n| Ver audit log | ❌ | ❌ | ✅ |\n| Gestionar admins | ❌ | ❌ | ✅ |\n| Ver analytics completos | ❌ | ✅ | ✅ |\n| Gestionar cola notificaciones | ❌ | ✅ | ✅ |\n\n---\n\n### 4.4 Flujo del Ciclo de Vida de Ticket\n\n`\n1. CREACIÓN\n   visitor_inquiry → trigger → support_ticket (status: 'new')\n   \n2. ASIGNACIÓN\n   Admin click \"Asignarme\" → assigned_to = admin_id\n   Status: 'new' → 'in_progress'\n   \n3. RESPUESTA\n   Admin envía respuesta → admin_responses\n   → Trigger notificación → admin_notifications_queue\n   \n4. SEGUIMIENTO\n   Usuario ve respuesta → viewed_by_user = true\n   Usuario click enlace → link_clicked = true\n   \n5. RESOLUCIÓN\n   Admin marca resolved → status = 'resolved'\n   \n6. CIERRE\n   Auto: 7 días después → status = 'closed'\n   Manual: Admin → status = 'closed'\n`\n\n---\n\n### 4.5 Instrucciones de Testing\n\n**Tests de Triggers:**\n`sql\n-- Test 1: Crear ticket genera notificación\nINSERT INTO admin_responses (...) VALUES (...);\nSELECT * FROM notifications WHERE type = 'admin_response';\n\n-- Test 2: Reasignación registra audit\nSELECT reassign_ticket('uuid', 'new-admin-uuid');\nSELECT * FROM admin_audit_log WHERE action = 'ticket_reassigned';\n\n-- Test 3: Activity cache se actualiza\nINSERT INTO appointments (...) VALUES (...);\nSELECT * FROM user_activity_cache WHERE user_id = 'uuid';\n`\n\n**Tests de RLS:**\n`sql\n-- Como support_agent\nSET SESSION \"request.jwt.claims\" TO '{\"sub\":\"agent-uuid\", \"role\":\"authenticated\"}';\nSELECT * FROM support_tickets; -- Solo assigned_to = agent-uuid\n\n-- Como super_admin\nSET SESSION \"request.jwt.claims\" TO '{\"sub\":\"admin-uuid\", \"role\":\"authenticated\"}';\nSELECT * FROM admin_audit_log; -- Ve todo\n`\n\n---\n\n### 4.6 Testing del Sistema de Retry\n\n`typescript\n// Test de retry automático\nconst testRetrySystem = async () => {\n  // 1. Crear notificación fallida\n  const notification = await supabase\n    .from('admin_notifications_queue')\n    .insert({\n      response_id: 'test-response-id',\n      recipient_user_id: 'test-user-id',\n      status: 'failed',\n      retry_count: 1,\n      max_retries: 3,\n      error_message: 'Test error'\n    });\n    \n  // 2. Simular trigger\n  await supabase.rpc('schedule_notification_retry');\n  \n  // 3. Verificar que status = 'queued' y retry_count = 2\n  const updated = await supabase\n    .from('admin_notifications_queue')\n    .select('*')\n    .eq('id', notification.data[0].id);\n    \n  console.log('Status:', updated.data[0].status); // Should be 'queued'\n  console.log('Retry count:', updated.data[0].retry_count); // Should be 2\n};\n`\n\n---\n\n## 5. 🗂️ Estructura de Archivos Actualizada\n\n`\nsrc/\n├── app/\n│   ├── admin/\n│   │   ├── tickets/\n│   │   │   ├── page.tsx           # Bandeja de tickets\n│   │   │   └── [id]/\n│   │   │       └── page.tsx       # Vista detalle ticket\n│   │   ├── notifications/\n│   │   │   └── page.tsx           # Cola de notificaciones\n│   │   ├── audit/\n│   │   │   └── page.tsx           # Log de auditoría\n│   │   └── users/\n│   │       ├── page.tsx           # Lista usuarios\n│   │       └── [id]/\n│   │           └── page.tsx       # Actividad usuario\n│   └── api/\n│       └── admin/\n│           ├── tickets/\n│           ├── responses/\n│           ├── notifications/\n│           └── audit-log/\n├── components/\n│   └── admin/\n│       ├── TicketsInbox.tsx\n│       ├── ResponseForm.tsx\n│       ├── UserActivityView.tsx\n│       ├── AuditLog.tsx\n│       └── NotificationQueue.tsx\n└── types/\n    └── admin.ts                   # TypeScript interfaces\n`\n\n---\n\n## 6. 🎯 Plan de Implementación Actualizado\n\n### Fase 4A: Tablas Base + Extensiones Críticas 🔴\n- [x] Crear `support_tickets` + `auto_close_after_days`\n- [x] Crear `admin_responses` mejorada + tracking fields\n- [x] Implementar triggers de actualización de ticket\n- [x] Crear RLS policies\n- **Commit:** \"database: Crear support_tickets y admin_responses con extensiones\"\n\n### Fase 4B: Permisos, Auditoría + Workflows 🟡\n- [ ] Crear `admin_roles` + `admin_audit_log`\n- [ ] Implementar función `reassign_ticket()`\n- [ ] Implementar función `auto_close_resolved_tickets()`\n- [ ] Crear RLS policies avanzadas\n- **Commit:** \"database: Crear admin_roles, audit_log y workflows\"\n\n### Fase 4C: Cache, Notificaciones + UI Prep 🟡\n- [ ] Crear `user_activity_cache` + `admin_notifications_queue`\n- [ ] Implementar triggers de cache y retry\n- [ ] Crear Worker process para queue\n- [ ] Documentar APIs necesarias\n- **Commit:** \"database: Crear cache, notification queue y workers\"\n\n### Fase 4D: UI Components + Testing 🟢\n- [ ] Crear componentes admin React\n- [ ] Implementar APIs REST admin\n- [ ] Testing completo de triggers y RLS\n- [ ] Documentación final con diagramas\n- **Commit:** \"feat: Implementar UI admin completa con testing\"\n\n---\n\n## ✅ CONFIRMACIÓN DE INTEGRACIÓN\n\nEstas extensiones críticas han sido **oficialmente integradas** al plan de implementación como requisitos obligatorios. No son opcionales sino **especificaciones técnicas completas** para asegurar:\n\n- ✅ **Escalabilidad** del sistema admin\n- ✅ **Trazabilidad** completa de acciones\n- ✅ **Usabilidad** profesional del panel\n- ✅ **Confiabilidad** del sistema de notificaciones\n- ✅ **Compliance** con auditoría administrativa\n- ✅ **Performance** optimizada para operaciones admin\n\nEl sistema resultante será **enterprise-ready** y preparado para crecimiento.\n"
